{
  "name": "webpack_sand",
  "version": "1.0.0",
  "description": "sandbox of webpacking",
  "main": "index.js",
  "scripts": {
    "build": "webpack",
    "build:dev" : "webpack --mode=development --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "npm run build:dev & nodemon server --ignore src/ --ignore dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "faker": "^5.1.0",
    "webpack": "^5.4.0",
    "webpack-cli": "^4.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
